apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-configs.fullname" . }}-test-connection"
  labels:
    {{- include "db-configs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-configs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
